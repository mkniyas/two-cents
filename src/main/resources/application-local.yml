# Local development configuration
# This profile is activated when running locally

spring:
  application:
    name: poc-service:1.0.0

logging:
  level:
    root: INFO
    com.patientpoint.poc-service: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

