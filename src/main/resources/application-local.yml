server:
  port: 8080
spring:
  application:
    name: two-cents
  profiles:
    active: local
  kafka:
    bootstrap-servers: localhost:29092
    consumer:
      group-id: two-cents-group
      auto-offset-reset: earliest
logging:
  structured:
    level:
      root: debug
      com.patientpoint: debug
